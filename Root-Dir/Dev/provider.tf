provider "azurerm" {
  features {}
  # subscription_id = "your-subscription-id" # Optional, if not set, the default subscription will be used
  # client_id       = "your-client-id"       # Optional, if not set, the default client ID will be used
  # client_secret   = "your-client-secret"   # Optional, if not set, the default client secret will be used
  # tenant_id       = "your-tenant-id"       # Optional, if not set, the default tenant ID will be used
  
}